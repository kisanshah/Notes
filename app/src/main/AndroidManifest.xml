<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notes">

    <application
        android:name=".activity.FirebaseOffline"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes">
        <activity
            android:name=".activity.views.ForgetActivity"
            android:parentActivityName=".activity.views.SignInActivity" />
        <activity
            android:name=".activity.views.EditNote"
            android:parentActivityName=".activity.views.MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activity.views.AddNote"
            android:parentActivityName=".activity.views.MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activity.views.SignUpActivity"
            android:parentActivityName=".activity.views.SignInActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activity.views.SignInActivity"
            android:parentActivityName=".activity.views.SignInActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.views.MainActivity" />
    </application>

</manifest>